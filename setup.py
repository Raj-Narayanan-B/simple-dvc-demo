from setuptools import setup, find_packages

setup(
    name = "src",
    version = "0.0.1",
    description = "It's an MLOPS package for wine quality",
    author = "Raj",
    packages = find_packages(),
    license = "MIT"
    )